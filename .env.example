###############################################################################
# GPT‑Review ▸ Example environment file
###############################################################################
# Copy this file to `.env` in the project root and customise as needed.
# The `install.sh` script *does not* read `.env`, but most shells will export
# these variables automatically when you run `source .env`.
#
# ---------------------------------------------------------------------------
#  Browser profile
# ---------------------------------------------------------------------------
# Path where Chromium stores cookies and cache.  If the directory does not
# exist, GPT‑Review creates it.  Keeping a persistent profile means you only
# need to log in to chat.openai.com *once*.
#
# Default: ~/.cache/gpt-review/chrome
#
# GPT_REVIEW_PROFILE="$HOME/.cache/gpt-review/chrome"

# ---------------------------------------------------------------------------
#  Headless mode
# ---------------------------------------------------------------------------
# Set to *any* non‑empty value to force Chromium’s new headless mode.
# Useful for CI / Docker.  You still need an X11 display inside CI runners
# (we use `xvfb-run` on GitHub Actions).
#
# GPT_REVIEW_HEADLESS=1

# ---------------------------------------------------------------------------
#  Custom log directory
# ---------------------------------------------------------------------------
# Override the directory where rotating log files are written.  Relative paths
# are resolved against the current working directory.
#
# GPT_REVIEW_LOG_DIR="logs"

# ---------------------------------------------------------------------------
#  Network proxy (optional)
# ---------------------------------------------------------------------------
# Uncomment & adjust if you must go through a corporate proxy.
# The var is read by the *webdriver‑manager* downloader.
#
# HTTPS_PROXY="http://proxy.example.com:3128"
# HTTP_PROXY="$HTTPS_PROXY"

# ---------------------------------------------------------------------------
#  Other tunables
# ---------------------------------------------------------------------------
# Maximum characters per error‑log chunk (default: 15000).
# Larger values risk exceeding ChatGPT’s token limit.
#
# GPT_REVIEW_CHUNK_SIZE=15000
#
# Timeout for run‑command (seconds). Same as `--timeout` CLI flag.
#
# GPT_REVIEW_COMMAND_TIMEOUT=300
