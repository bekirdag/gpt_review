===============================================================================
🎯  PROJECT GOAL
===============================================================================
Modernise the code‑base for **Python 3.12**, achieving 90 % pytest coverage and
eliminating all `DeprecationWarning`s.

* Migrated libraries:
    - `flask.ext.*`            ➔  replace with standalone packages
    - `asyncio.get_event_loop` ➔  use `asyncio.run`
    - Any `asyncio.coroutine`  ➔  native `async def`

* CI target in `.github/workflows/ci.yml` *must* pass green with:
      python-version: ["3.10", "3.11", "3.12"]

-------------------------------------------------------------------------------
🔨  ACCEPTANCE CRITERIA
-------------------------------------------------------------------------------
1.  `pytest -q` reports **≥ 90 % coverage**.
2.  No warnings when running tests under **Python 3.12**.
3.  `flake8` passes with **zero** errors or warnings.
4.  All existing CLI commands keep identical behaviour (BC‑safe).

-------------------------------------------------------------------------------
📋  WORKFLOW RULES
-------------------------------------------------------------------------------
* **One JSON patch per reply** – see schema below.
* Work *one file at a time* (commit history must stay readable).
* **After sending a patch, ask me to _continue_** before proceeding.
* After each patch, set `"status": "in_progress"` until the **last** patch,
  then use `"status": "completed"`.

```jsonc
{ "op": "create|update|delete|rename|chmod",
  "file": "path/inside/repo",
  // create / update
  "body": "full file contents",
  "body_b64": "<base64>",    // optional binary
  // rename
  "target": "new/path",
  // chmod
  "mode": "755",
  "status": "in_progress|completed"
}
