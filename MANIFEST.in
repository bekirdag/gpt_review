###############################################################################
# GPT‑Review ▸ Source distribution manifest
###############################################################################
# Controls which **additional** files are included in the *sdist* (tar.gz)
# built by `python -m build` or `pip install .`.
#
# Wheels (.whl) are governed by pyproject.toml:
#   [tool.setuptools.package-data]  and  [tool.setuptools]
#
# Guideline: Include **everything required to run tests, use the package,
# and reproduce CI locally** – licenses, docs, example files, schemas, and
# helper scripts.
###############################################################################

# ─────────────────────────────────────────────────────────────────────────────
# 1) Licensing & top‑level docs
# ─────────────────────────────────────────────────────────────────────────────
include LICENSE
include README.md
include CHANGELOG.md
include CONTRIBUTING.md

# ─────────────────────────────────────────────────────────────────────────────
# 2) Top‑level Python modules (also listed in pyproject.toml [tool.setuptools].py-modules)
# ─────────────────────────────────────────────────────────────────────────────
include review.py
include apply_patch.py
include patch_validator.py
include logger.py

# ─────────────────────────────────────────────────────────────────────────────
# 3) JSON schema shipped inside the package
#    (Wheel inclusion handled via [tool.setuptools.package-data])
# ─────────────────────────────────────────────────────────────────────────────
recursive-include gpt_review *.json

# ─────────────────────────────────────────────────────────────────────────────
# 4) Example instruction and environment files
# ─────────────────────────────────────────────────────────────────────────────
include example_instructions.txt
include .env.example

# ─────────────────────────────────────────────────────────────────────────────
# 5) Shell helper scripts (distributed with sdist)
# ─────────────────────────────────────────────────────────────────────────────
include software_review.sh
include cookie_login.sh

# ─────────────────────────────────────────────────────────────────────────────
# 6) Tests (so downstream packagers/users can run `pytest`)
# ─────────────────────────────────────────────────────────────────────────────
recursive-include tests *.py

# ─────────────────────────────────────────────────────────────────────────────
# 7) Exclude local rubbish just in case
# ─────────────────────────────────────────────────────────────────────────────
global-exclude *.pyc
global-exclude __pycache__/
global-exclude *.log
